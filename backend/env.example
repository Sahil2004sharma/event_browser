# Copy to .env and adjust. Prisma CLI requires DATABASE_URL when running migrations.
DATABASE_URL="file:./dev.db"

# MongoDB (email/password auth). Encode @ as %40 and $ as %24 inside the password.
# MONGODB_URI="mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/?retryWrites=true&w=majority"
